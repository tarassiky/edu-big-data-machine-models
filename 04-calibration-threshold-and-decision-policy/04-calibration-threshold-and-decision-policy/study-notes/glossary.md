# Глоссарий незнакомых вещей (обязательно)

Обновляйте этот файл **по ходу выполнения** ноутбуков, а не в конце.

Минимальные требования:
- добавить не менее 3 новых терминов на каждый ноутбук;
- для каждого термина указать простое объяснение своими словами;
- указать, где термин встретился в работе;
- добавить источник (URL, учебник, статья или ссылка на внутреннюю заметку).

| Термин | Простое объяснение своими словами | Где встретился в ЛР | Источник |
|---|---|---|---|
| `VarianceThreshold` | Удаляет признаки почти без разброса, которые обычно не помогают модели различать классы. | Notebook 1, шаг `Filter-методы`. | scikit-learn User Guide: Feature selection. |
| `mutual_info_classif` | Оценивает, насколько признак связан с таргетом, даже если связь нелинейная. | Notebook 1, сравнение filter-методов. | scikit-learn API: `mutual_info_classif`. |
| `RFE` | Итерируется по признакам и постепенно убирает наименее полезные по мнению модели. | Notebook 2, wrapper-подходы. | scikit-learn API: `RFE`. |
| `Permutation Importance` | Смотрит, насколько портится метрика, если случайно перемешать один признак. | Notebook 2 и 3, embedded/permutation анализ. | scikit-learn User Guide: permutation importance. |
| `Overlap` | Количество общих элементов между двумя множествами. В контексте отбора признаков — сколько признаков совпадает в двух разных shortlist. | Задание 1 и 2, сравнение shortlist. | Внутреннее: изучено в ходе выполнения ЛР01. |
| `Jaccard Index` | Метрика сходства множеств: отношение размера пересечения к размеру объединения. Показывает, насколько похожи два набора признаков. | Задание 2, попарное сравнение конфигураций. | https://en.wikipedia.org/wiki/Jaccard_index |
| `Stability Grid` | Сетка параметров для проверки устойчивости отбора признаков. Позволяет увидеть, как меняется shortlist при разных порогах и количестве признаков. | Задание 1 и 3, анализ устойчивости. | Внутреннее: введено в ходе выполнения ЛР01. |
| `Pairwise Similarity` | Попарное сравнение сходства между всеми конфигурациями. Используется для кластеризации или визуализации похожих наборов признаков. | Задание 2, расчет overlap и Jaccard. | Внутреннее: введено в ходе выполнения ЛР01. |
| `SequentialFeatureSelector (SFS)` | Последовательный отбор признаков: на каждом шаге добавляется признак, который лучше всего улучшает метрику модели (forward selection). | Notebook 2, wrapper-методы. | scikit-learn API: `SequentialFeatureSelector`. |
| `RFE (Recursive Feature Elimination)` | Рекурсивное удаление признаков: модель обучается, самые слабые признаки удаляются, процесс повторяется. | Notebook 2, wrapper-методы. | scikit-learn API: `RFE`. |
| `L1-регуляризация (Lasso)` | Добавляет штраф за большие коэффициенты, обнуляя неважные признаки. Используется для встроенного отбора признаков. | Notebook 2, embedded-методы. | scikit-learn API: `LogisticRegression` с `penalty='l1'`. |
| `RandomForest Feature Importance` | Оценивает важность признаков на основе деревьев решений: чем чаще признак используется для разделения, тем он важнее. | Notebook 2, embedded-методы. | scikit-learn API: `RandomForestClassifier.feature_importances_`. |
| `Stability Rate` | Доля запусков, в которых признак был отобран при разных random_state. Показывает, насколько устойчив отбор признаков. | Задание 2, стабильность отбора. | Внутреннее: введено в ходе выполнения ЛР01. |
| `set_D_robust` | Набор признаков, отобранных с высокой стабильностью (stability_rate >= 0.6). Рекомендуется для продакшн-систем. | Задание 3, формирование устойчивого набора. | Внутреннее: введено в ходе выполнения ЛР01. |
| `Wrapper-методы` | Методы отбора признаков, которые используют модель для оценки важности признаков (RFE, SFS). Учитывают взаимодействие признаков. | Notebook 2, wrapper-подходы. | Внутреннее: изучено в ходе выполнения ЛР01. |
| `Embedded-методы` | Методы отбора признаков, встроенные в процесс обучения модели (L1-регуляризация, RandomForest Importance). Быстрее wrapper-методов. | Notebook 2, embedded-подходы. | Внутреннее: изучено в ходе выполнения ЛР01. |
| `LogisticRegression` | Линейная модель для бинарной классификации. Возвращает вероятности принадлежности к классу. Быстрая и интерпретируемая. | Notebook 3, сравнение моделей. | scikit-learn API: `LogisticRegression`. |
| `RandomForest` | Ансамбль деревьев решений. Устойчив к переобучению, учитывает нелинейные взаимодействия признаков. | Notebook 3, сравнение моделей. | scikit-learn API: `RandomForestClassifier`. |
| `LinearSVC` | Линейный метод опорных векторов (Support Vector Classifier) для бинарной классификации. Эффективен для линейно разделимых данных, быстрее RandomForest. | Notebook 3 (ЛР01) и Notebook 2 (ЛР02). | scikit-learn API: `LinearSVC`. |
| `MLPClassifier` | Нейронная сеть с одним скрытым слоем. Может улавливать сложные нелинейные зависимости, но требует много данных. | Notebook 3, опциональный блок. | scikit-learn API: `MLPClassifier`. |
| `Threshold Tuning` | Подбор порога классификации для оптимизации метрик (precision/recall/f1). Позволяет настроить модель под бизнес-задачи. | Задание 1, тюнинг порога. | Внутреннее: изучено в ходе выполнения ЛР01. |
| `Cross-Validation (CV)` | Метод оценки стабильности модели путём разбиения данных на несколько частей (фолдов). Помогает проверить, не переобучена ли модель. | Задание 2, CV-проверка. | scikit-learn API: `StratifiedKFold`. |
| `Segmental Error Analysis` | Анализ ошибок модели в различных сегментах данных (например, по возрасту или кредитному рейтингу). Позволяет выявить слабые места модели. | Задание 3, сегментный анализ. | Внутреннее: изучено в ходе выполнения ЛР01. |
| `Precision` | Доля правильных положительных предсказаний среди всех положительных предсказаний. Показывает, насколько модель "не ошибается" в положительных прогнозах. | Задание 1, метрики при тюнинге порога. | Внутреннее: изучено в ходе выполнения ЛР01. |
| `Recall` | Доля правильно предсказанных положительных объектов среди всех реально положительных. Показывает, сколько положительных объектов модель "поймала". | Задание 1, метрики при тюнинге порога. | Внутреннее: изучено в ходе выполнения ЛР01. |
| `F1-score` | Среднее гармоническое между precision и recall. Используется, когда важен баланс между этими двумя метриками. | Notebook 1-3, основная метрика качества. | Внутреннее: изучено в ходе выполнения ЛР01. |
| `Partial Dependence` | Показывает, как меняется среднее предсказание модели при изменении одного признака, усредняя влияние всех остальных. Помогает понять направление и форму влияния признака. | Notebook 1 (ЛР02), partial dependence для числовых признаков. | scikit-learn User Guide: partial dependence. |
| `Native Importance` | Встроенная в модель оценка важности признаков. Для LogisticRegression — это абсолютные значения коэффициентов (coef_abs). Для RandomForest — feature_importances_. | Notebook 1 (ЛР02), глобальная интерпретация. | Внутреннее: изучено в ходе выполнения ЛР02. |
| `Coef_abs` | Абсолютное значение коэффициента логистической регрессии. Показывает силу влияния признака на предсказание (чем больше значение, тем важнее признак). | Notebook 1 (ЛР02), глобальная интерпретация LogisticRegression. | scikit-learn API: `LogisticRegression.coef_`. |
| `Score Delta` | Разница между максимальным и минимальным значением предсказания на partial dependence кривой. Показывает, насколько сильно признак влияет на предсказания модели. | Notebook 1 (ЛР02), partial dependence summary. | Внутреннее: изучено в ходе выполнения ЛР02. |
| `Non-decreasing` | Тип тренда на partial dependence кривой — кривая не убывает (монотонный рост или плато). | Notebook 1 (ЛР02), анализ трендов. | Внутреннее: изучено в ходе выполнения ЛР02. |
| `Non-increasing` | Тип тренда на partial dependence кривой — кривая не возрастает (монотонное убывание или плато). | Notebook 1 (ЛР02), анализ трендов. | Внутреннее: изучено в ходе выполнения ЛР02. |
| `Non-monotonic` | Тип тренда на partial dependence кривой, при котором зависимость не является монотонной (есть участки роста и убывания). | Notebook 1 (ЛР02), анализ трендов. | Внутреннее: изучено в ходе выполнения ЛР02. |
| `Global Importance Comparison` | Сравнение разных методов оценки важности признаков (native importance и permutation importance) для разных моделей. | Notebook 1 (ЛР02), глобальная интерпретация. | Внутреннее: введено в ходе выполнения ЛР02. |
| `Perturbation-анализ` | Метод локальной интерпретации, основанный на небольшом изменении значений признаков и наблюдении за тем, как меняется предсказание модели. | Notebook 2 (ЛР02), локальный разбор ошибок. | Внутреннее: изучено в ходе выполнения ЛР02. |
| `False Positive` | Ошибка классификации: модель предсказала положительный класс (1), но истинный класс — отрицательный (0). | Notebook 2 (ЛР02), локальный анализ ошибок. | Внутреннее: изучено в ходе выполнения ЛР02. |
| `False Negative` | Ошибка классификации: модель предсказала отрицательный класс (0), но истинный класс — положительный (1). | Notebook 2 (ЛР02), локальный анализ ошибок. | Внутреннее: изучено в ходе выполнения ЛР02. |
| `Decision Memo` | Краткий документ с практическими рекомендациями по выбору модели для конкретной задачи. Учитывает баланс качества и интерпретируемости. | Notebook 2 (ЛР02), задание 3. | Внутреннее: введено в ходе выполнения ЛР02. |
| `Local Explanation` | Объяснение конкретного предсказания модели (почему модель приняла такое решение для данного наблюдения). | Notebook 2 (ЛР02), локальный разбор ошибок. | Внутреннее: изучено в ходе выполнения ЛР02. |
| `Generalization Gap` | Разрыв между качеством модели на обучающей и валидационной выборках. Большой gap — признак переобучения. | Notebook 1 (ЛР03), аудит обобщения. | Внутреннее: изучено в ходе выполнения ЛР03. |
| `Train/Validation/Test Split` | Разделение данных на три части: train (обучение), validation (выбор гиперпараметров и моделей), test (финальная оценка). | Notebook 1 (ЛР03), честное разделение данных. | Внутреннее: изучено в ходе выполнения ЛР03. |
| `Validation Curve` | График, показывающий изменение качества модели на train и validation при изменении одного гиперпараметра. Помогает найти баланс между underfitting и overfitting. | Notebook 1 (ЛР03), validation curves. | scikit-learn User Guide: validation curves. |
| `Underfitting` | Состояние модели, когда она слишком простая и не может уловить закономерности в данных. На validation и train качество одинаково низкое. | Notebook 1 (ЛР03), анализ validation curves. | Внутреннее: изучено в ходе выполнения ЛР03. |
| `Overfitting` | Состояние модели, когда она слишком сложная и запоминает обучающие данные, но плохо обобщается на новые. Большой generalization gap. | Notebook 1 (ЛР03), анализ validation curves. | Внутреннее: изучено в ходе выполнения ЛР03. |
| `Feature Set Decision` | Выбор набора признаков для каждой модели отдельно на основе валидационных метрик. Не универсальный winner, а решение в связке с моделью. | Notebook 1 (ЛР03), выбор feature set. | Внутреннее: введено в ходе выполнения ЛР03. |
| `GridSearchCV` | Метод подбора гиперпараметров через полный перебор комбинаций с кросс-валидацией. Будет использован во втором ноутбуке. | Notebook 2 (ЛР03), GridSearchCV. | scikit-learn API: `GridSearchCV`. |
| `Generalization Gap` | Разрыв между качеством модели на обучающей и валидационной выборках. Большой gap — признак переобучения. | Notebook 1 (ЛР03), аудит обобщения. | Внутреннее: изучено в ходе выполнения ЛР03. |
| `Train/Validation/Test Split` | Разделение данных на три части: train (обучение), validation (выбор гиперпараметров и моделей), test (финальная оценка). Test не используется до самого конца. | Notebook 1 (ЛР03), честное разделение данных. | Внутреннее: изучено в ходе выполнения ЛР03. |
| `Validation Curve` | График, показывающий изменение качества модели на train и validation при изменении одного гиперпараметра. Помогает найти баланс между underfitting и overfitting. | Notebook 1 (ЛР03), validation curves. | scikit-learn User Guide: validation curves. |
| `Underfitting` | Состояние модели, когда она слишком простая и не может уловить закономерности в данных. На validation и train качество одинаково низкое. | Notebook 1 (ЛР03), анализ validation curves. | Внутреннее: изучено в ходе выполнения ЛР03. |
| `Overfitting` | Состояние модели, когда она слишком сложная и запоминает обучающие данные, но плохо обобщается на новые. Большой generalization gap. | Notebook 1 (ЛР03), анализ validation curves. | Внутреннее: изучено в ходе выполнения ЛР03. |
| `Feature Set Decision` | Выбор набора признаков для каждой модели отдельно на основе валидационных метрик. Не универсальный winner, а решение в связке с моделью. | Notebook 1 (ЛР03), выбор feature set. | Внутреннее: введено в ходе выполнения ЛР03. |
| `GridSearchCV` | Метод подбора гиперпараметров через полный перебор комбинаций с кросс-валидацией. Работает только на train для честной оценки. | Notebook 2 (ЛР03), GridSearchCV. | scikit-learn API: `GridSearchCV`. |
| `Pipeline` | Объект scikit-learn, который объединяет несколько шагов (препроцессинг + модель) в единый процесс. Гарантирует, что препроцессинг выполняется внутри CV, предотвращая утечку данных. | Notebook 2 (ЛР03), GridSearchCV. | scikit-learn API: `Pipeline`. |
| `Baseline Default` | Модель с настройками по умолчанию. Используется как отправная точка для сравнения с tuned-моделью. | Notebook 2 (ЛР03), финальное сравнение на test. | Внутреннее: введено в ходе выполнения ЛР03. |
| `Tuned Best` | Модель с гиперпараметрами, подобранными через GridSearchCV. Сравнивается с baseline на test. | Notebook 2 (ЛР03), финальное сравнение на test. | Внутреннее: введено в ходе выполнения ЛР03. |
| `Holdout` | Часть данных (обычно test), которая не используется ни для обучения, ни для выбора гиперпараметров. Служит для финальной честной оценки модели. | Notebook 2 (ЛР03), финальная проверка на test. | Внутреннее: изучено в ходе выполнения ЛР03. |
| `Data Leakage` | Ситуация, когда информация из test или validation случайно используется при обучении модели. Приводит к завышенным и нечестным метрикам. | Notebook 2 (ЛР03), предотвращение leakage через Pipeline. | Внутреннее: изучено в ходе выполнения ЛР03. |
| `Brier Score` | Средний квадрат разницы между предсказанной вероятностью и истинной меткой. Чем меньше значение, тем точнее вероятности. Формула: (1/N) * Σ(p_i - y_i)². | Notebook 1 (ЛР04), калибровка вероятностей. | scikit-learn API: `brier_score_loss`. |
| `LogLoss` | Логарифмическая функция потерь для вероятностных предсказаний. Сильно штрафует модель за уверенные ошибки. Формула: -1/N * Σ[y_i*log(p_i) + (1-y_i)*log(1-p_i)]. | Notebook 1 (ЛР04), калибровка вероятностей. | Внутреннее: изучено в ходе выполнения ЛР04. |
| `ECE (Expected Calibration Error)` | Среднее абсолютное отклонение между предсказанной вероятностью и фактической частотой события в бинах. Показывает, насколько хорошо откалибрована модель. Формула: Σ(n_b/N) * |conf(b) - acc(b)|. | Notebook 1 (ЛР04), калибровка вероятностей. | Внутреннее: изучено в ходе выполнения ЛР04. |
| `Calibration` | Процесс настройки вероятностей модели так, чтобы они лучше отражали реальную частоту событий. Например, если модель предсказывает 70%, то в 70% случаев событие должно происходить. | Notebook 1 (ЛР04), калибровка вероятностей. | Внутреннее: изучено в ходе выполнения ЛР04. |
| `Sigmoid Calibration` | Метод калибровки, использующий логистическую регрессию для преобразования сырых вероятностей. Предполагает монотонную зависимость между сырыми и откалиброванными вероятностями. | Notebook 1 (ЛР04), калибровка вероятностей. | scikit-learn API: `CalibratedClassifierCV` с `method='sigmoid'`. |
| `Isotonic Calibration` | Метод калибровки, использующий изотонную регрессию. Не предполагает монотонности сигмоиды, может давать более гибкую калибровку, но склонен к переобучению на малых выборках. | Notebook 1 (ЛР04), калибровка вероятностей. | scikit-learn API: `CalibratedClassifierCV` с `method='isotonic'`. |
| `Reliability Diagram` | График, показывающий, насколько предсказанные вероятности соответствуют фактическим частотам событий. Близость точек к диагонали (y=x) = хорошая калибровка. | Notebook 1 (ЛР04), анализ надежности. | Внутреннее: изучено в ходе выполнения ЛР04. |
| `Calibration Gap` | Разрыв между предсказанной вероятностью и фактической частотой события в конкретном бине. Большой gap указывает на систематическое смещение вероятностей. | Notebook 1 (ЛР04), анализ надежности. | Внутреннее: изучено в ходе выполнения ЛР04. |
| `PR-AUC` | Площадь под кривой Precision-Recall. Оценивает качество модели на несбалансированных данных. В отличие от ROC-AUC, чувствительна к дисбалансу классов. | Notebook 1 (ЛР04), калибровка вероятностей. | scikit-learn API: `average_precision_score`. |
| `Uncalibrated` | Модель без калибровки вероятностей. Использует сырые вероятности, которые могут быть систематически смещены. | Notebook 1 (ЛР04), сравнение вариантов калибровки. | Внутреннее: изучено в ходе выполнения ЛР04. |
| `Calibrated Best` | Лучший калиброванный вариант модели, выбранный по минимальному ECE на валидации. Используется как основной кандидат для выбора порога во втором ноутбуке. | Notebook 1 (ЛР04), выбор calibrated_best. | Внутреннее: введено в ходе выполнения ЛР04. |
| `Expected Cost` | Ожидаемая стоимость ошибок модели на одном наблюдении. Рассчитывается как (FP_cost * FP_rate + FN_cost * FN_rate) / N. Позволяет учитывать реальные потери от ошибок. | Notebook 2 (ЛР04), выбор порога. | Внутреннее: изучено в ходе выполнения ЛР04. |
| `Cost-Sensitive Decision` | Правило принятия решений, учитывающее разную стоимость ошибок FP и FN. Позволяет настроить модель под бизнес-задачи (например, FN в 5 раз дороже FP). | Notebook 2 (ЛР04), выбор порога. | Внутреннее: изучено в ходе выполнения ЛР04. |
| `Guardrail` | Ограничение-страховка (например, `recall >= 0.60`), которое защищает от слишком рискованных порогов. Гарантирует минимальный уровень полноты модели. | Notebook 2 (ЛР04), выбор порога. | Внутреннее: изучено в ходе выполнения ЛР04. |
| `Policy` | Правило решения, которое включает модель, вариант калибровки и порог классификации. Определяет, как принимать решения на практике. | Notebook 2 (ЛР04), выбор правила решения. | Внутреннее: введено в ходе выполнения ЛР04. |
| `Policy Test Report` | Отчёт о применении выбранного правила решения на тестовой выборке. Содержит метрики качества и стоимость ошибок. | Notebook 2 (ЛР04), финальная проверка на test. | Внутреннее: введено в ходе выполнения ЛР04. |
| `Segment Policy Audit` | Анализ стоимости ошибок правила решения в разных сегментах данных (например, по возрасту или кредитному рейтингу). Позволяет выявить группы, где модель работает хуже всего. | Notebook 2 (ЛР04), сегментный аудит. | Внутреннее: введено в ходе выполнения ЛР04. |
| `Cost per 100` | Ожидаемая стоимость ошибок на 100 наблюдений. Удобная метрика для бизнес-интерпретации и сравнения разных правил решения. | Notebook 2 (ЛР04), policy test report. | Внутреннее: изучено в ходе выполнения ЛР04. |
| `Threshold Sweep` | Перебор всех возможных порогов классификации для выбора оптимального по заданному критерию (например, `min expected_cost` при `recall >= 0.60`). | Notebook 2 (ЛР04), выбор порога. | Внутреннее: изучено в ходе выполнения ЛР04. |
| `FP Cost / FN Cost` | Стоимость ложноположительной (FP) и ложноотрицательной (FN) ошибок. Используется для расчёта ожидаемой стоимости и выбора оптимального порога. | Notebook 2 (ЛР04), выбор порога. | Внутреннее: изучено в ходе выполнения ЛР04. |
| `Calibrated Best` | Лучший калиброванный вариант модели, выбранный по минимальному ECE на валидации. Используется как основной кандидат для выбора порога во втором ноутбуке. | Notebook 1 и 2 (ЛР04), выбор calibrated_best. | Внутреннее: введено в ходе выполнения ЛР04. |